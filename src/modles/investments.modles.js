const dbConnection = require('../db/connections.db');

const addInvestmentModel = async (investmentData) => {

}

const squareOffInvestmentModel = async (investmentId) => {
}

module.exports = {
    addInvestmentModel,
    squareOffInvestmentModel
};